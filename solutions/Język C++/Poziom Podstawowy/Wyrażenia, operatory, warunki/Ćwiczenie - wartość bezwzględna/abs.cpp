// Zadanie:
// uzupełnij funkcję int Abs (int value) tak, aby zwracała wartość bezwzględną wartości przekazanej do funkcji w parametrze value. 

int Abs( int value )
{
    if (value > 0) return value;
    else return abs(value);
}
